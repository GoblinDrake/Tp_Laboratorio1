/** \brief Modifica el primer numero
 *
 * \param recibe el numero para cambiarlo
 * \return retorna el numero modificado
 *
 */
float pedir1(float);
/** \brief Modifica el segundo numero
 *
 * \param recibe el numero para cambiarlo
 * \return retorna el numero cambiao
 *
 */
float pedir2(float num2);
/** \brief Suma dos numeros
 *
 * \param recibe dos numeros para sumarlos
 * \return retorna la suma de estos
 *
 */
float sumar(float,float);
/** \brief Resta dos numeros
 *
 * \param recibe dos numeros para restar el primero menos el segundo
 * \return retorna la resta del primero menos el segundo
 *
 */
float restar(float,float);
/** \brief Divide dos numeros
 *
 * \param recibe dos numeros para dividir el primero por el segundo
 * \return retorna la division de los dos numeros
 *
 */
float dividir(float,float);
/** \brief Multiplica dos numeros
 *
 * \param recibe dos numeros para multiplicarlos
 * \return retorna la multiplicacion de los dos numeros
 *
 */
float multiplicar(float,float);
/** \brief Calcula el factorial del primer numero
 *
 * \param recibe el primer y solo el primer numero para sacar su factorial
 * \return retorna el factorial de ese numero
 *
 */
float factorial(float);
/** \brief valida si el primer numero no es 0
 *
 * \param recibe el primer numero y se fija si no es 0
 * \return retorna el numero distinto a 0
 *
 */
float validar(float);
/** \brief valida si el segundo numero no es 0
 *
 * \param recibe el segundo numero y se fija si no es 0
 * \return retorna el numero distinto a 0
 *
 */
float validar2(float);


